<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quillora</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
 <link rel="stylesheet" href="styles/index.css">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat+Alternates:wght@400;500;700&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;700&display=swap" rel="stylesheet">
</head>
<body>
    <%- include("partials/header.ejs")%>
  
    <section class="image-section">
        <img src="assets/home1.jpg" alt="Your Image">
        <div class="text-overlay">
            <h4 class="ml2">A space for you thoughts on every chapter.</h4>
        </div>
    </section>
    <section class="Recently-posted">
        <div class="recent-title"><h2>Fresh Picks for Your Reading List!</h2></div>
        <div class="slider-container">
            <div class="arrow left-arrow" onclick="prevSlide()">&#10094;</div> <!-- Left Arrow -->
    
            <div class="card-container">
                <!-- Iterate through the 'home' data -->
                <% if (home && home.length > 0) { %>

                    <% home.forEach(function(item) { %>
                        <div class="book-card" data-post-id="<%= item.id %>">
                            <% if (item.cover_image) { %>
                                <img 
                                src="data:image/jpeg;base64,<%= item.cover_image.toString('base64') %>" 
                                alt="<%= item.title %> cover" 
                                class="book-image">
                            <% } else { %>
                                <img 
                                src="assets/none.jpg" 
                                alt="Default book cover" 
                                class="book-image">
                            <% } %>
                            
                            <div class="book-info">
                                <h3><%= item.title %></h3>
                                <p class="notes">Notes by <%= item.user_name %></p>
                            </div>
                        </div>
                    <% }) %>
                    
                <% } else { %>
                    <!-- Fallback Book Card if no data is available -->
                    <div class="book-card">
                        <img src="assets/negotiation-book.jpg" alt="You Can Negotiate Anything - Herb Cohen" class="book-image">
                        <div class="book-info">
                            <h3>You Can Negotiate Anything - by Herb Cohen</h3>
                            <p class="notes">Notes by kavya_quoters</p>
                        </div>
                    </div>
                <% } %>
            </div>
    
            <div class="arrow right-arrow" onclick="nextSlide()">&#10095;</div> <!-- Right Arrow -->
        </div>
    
    </section>
    <%- include("partials/footer.ejs")%>
    <script src="js/home.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/2.0.2/anime.min.js"></script>
   
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

   
</body>
</html>